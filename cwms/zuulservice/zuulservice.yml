
zuul:
  prefix:  /api
  routes:
    layoutservice: /layout/**
    authservice: /auth/**

# Expose * so we can access /actuator/routes
management:
  endpoints:
    web:
      exposure:
        include: "*"

customized:
    source: github
    version: 1.0
    lastUpdate: 8/2/2019
    profile: default

